# Handoff — 8 กันยายน 2026

อ่านไฟล์นี้คู่กับ `/CLAUDE.md` ก่อนเริ่มงาน และใช้ `docs/HANDOFF-2026-09-07.md` เมื่อต้องการประวัติงานก่อนหน้านี้แบบละเอียด

## สถานะปัจจุบัน

- Production: <https://rozerothai.com>
- Branch: `master`
- application commit ที่อยู่บน production: `875972b`
- handoff นี้เป็น docs-only commit หลัง application commit ข้างต้น
- commit ล่าสุดขึ้น production แล้ว และ purge Cloudflare แล้ว
- รอบตรวจล่าสุด: Vitest **628/628 ผ่าน**, TypeScript ผ่าน, Next.js production build ผ่าน

## งานที่ทำต่อจาก handoff วันที่ 7 ก.ย.

### 1. UX/UI audit และปรับ neon arcade

เป้าหมายคือเพิ่มกลิ่น neon arcade โดยไม่ลด readability, accessibility, SEO หรือ performance

#### `6eb6838 fix(ux): compact mobile shell and monster picker`

- ลดความสูงของ mobile header/subnav และจัด shell ให้ใช้พื้นที่จอคุ้มขึ้น
- ปรับ monster picker ให้ค้นหา/เลือกบนมือถือสะดวกขึ้น
- ตรวจ viewport 320px และ 390px แล้วไม่มี horizontal overflow
- footer ยังคงเป็น SSR links และ canonical ไม่เปลี่ยน

#### `1f681bc style: make neon accents functional and calmer`

- ลด neon ที่เป็น decoration ล้วน และใช้สีเน้นกับ interaction/state ที่มีความหมาย
- คุม glow, contrast และ motion ไม่ให้รบกวนการอ่านหรือผู้ใช้ที่ลด animation
- ไม่มี asset หรือ JavaScript เพิ่ม
- build size/route behavior ไม่ถอยหลัง

ทั้งสอง commit ขึ้น production และตรวจผ่าน Cloudflare แล้วก่อนเริ่มงานไกด์

### 2. ไกด์ใหม่ 2 หน้า

commit: `875972b content: add forging and potion guides`

#### ทำอาวุธธาตุ Blacksmith

- Route: `/guides/elemental-weapons`
- File: `app/guides/elemental-weapons/page.tsx`
- ครอบคลุม:
  - ของที่ต้องเตรียม: สกิลตีอาวุธ, สูตรอาวุธ, ค้อน, Anvil และหินธาตุ
  - ธาตุไฟ = Flame Heart
  - ธาตุน้ำ = Mystic Frozen
  - ธาตุลม = Rough Wind
  - ธาตุดิน = Great Nature
  - สูตรเปลี่ยนวัตถุดิบย่อย 10 ชิ้นเป็นหินธาตุ
  - ขั้นตอนตีและปัจจัยเพิ่มโอกาสสำเร็จ
  - เตือนว่า Star Crumb ไม่จำเป็นต่อการใส่ธาตุและทำให้โอกาสสำเร็จลดลง
- เชื่อมไป `/guides/elements`, `/tools/damage` และหน้า item ของหินแต่ละก้อน

#### ทำยา Alchemist

- Route: `/guides/potion-crafting`
- File: `app/guides/potion-crafting/page.tsx`
- ครอบคลุม:
  - Potion Research Lv.5 เพื่อปลด Prepare Potion
  - ใช้ Medicine Bowl/Mortar Bowl 1 ชิ้นต่อการลอง
  - ต้องมีตำราตรงหมวด แต่ตำราไม่ถูกใช้หมด
  - สูตร Alchemist Brewing 20 รายการ แยกเป็น:
    - ยาฟื้นและยาทั่วไป 6
    - ไอเทมเคมี 6
    - Slim Potion 3
    - ยาต้านธาตุ 4
    - Embryo 1
  - ปัจจัยเพิ่มอัตราสำเร็จและคำเตือนว่าสูตรเปอร์เซ็นต์ชุมชนยังอิง TWRO

#### การเชื่อมระบบและ SEO

- เพิ่มการ์ดทั้งสองใน `app/guides/page.tsx`
- เพิ่มเมนูใน `lib/nav-links.ts`
- sitemap ดึง route จาก nav อัตโนมัติ จึงมีสอง URL ใหม่โดยไม่ hardcode ซ้ำ
- ทั้งสองหน้าเป็น static HTML ขนาด route 209 B / First Load JS 96.2 kB
- มี metadata title/description, H1 เดียว, Breadcrumb JSON-LD, internal links และ source note
- แหล่งข้อมูลหลัก: local RO ZERO DATABASE export และ client-derived skill data
- ลิงก์แหล่งสูตร: <https://rozerodb.com/recipes>
- หลีกเลี่ยงการใส่เปอร์เซ็นต์สำเร็จตายตัว เพราะ Forge/Brewing simulator ระบุฐานสูตร TWRO ไม่ใช่ประกาศทางการ RO Zero Global

## Verification และ deploy

คำสั่งที่ผ่าน:

```powershell
npx tsc --noEmit
npm test
npm run build
```

ผล:

- 64 test files ผ่าน
- 628 tests ผ่าน
- production build สร้าง `/guides/elemental-weapons` และ `/guides/potion-crafting` เป็น static routes

Deploy:

1. `git push origin master` สำเร็จ: `1f681bc..875972b master -> master`
2. deploy รอบแรกถูก trigger แต่ origin ยัง 404 จน guard timeout 20 นาที
3. ตรวจ `HEAD == origin/master == 875972b`; Coolify deployments endpoint คืน array ว่าง
4. retry deploy หนึ่งครั้งด้วยหน้า potion เป็น marker
5. รอบ retry สำเร็จ: `origin serves the new build`, `purged everything`, `told Telegram`

ตรวจผ่าน public CDN หลัง deploy:

| URL | HTTP | Content | Canonical |
|---|---:|---|---|
| `/guides/elemental-weapons` | 200 | พบ H1/ข้อความใหม่ | `https://rozerothai.com/guides/elemental-weapons` |
| `/guides/potion-crafting` | 200 | พบ H1/ข้อความใหม่ | `https://rozerothai.com/guides/potion-crafting` |

## จุดที่ควรตรวจ/ทำต่อ

1. รับ feedback จากผู้เล่นเรื่องคำเรียกวัตถุดิบและตำราใน client ภาษาไทย แล้วเพิ่ม alias ไทยโดยยึด `docs/PLAYER_GLOSSARY.md`
2. หากจะทำ calculator Forge/Brewing ต้องยืนยันสูตรกับ ROZ จริงก่อน ห้ามนำสูตร TWRO มาแสดงเป็นค่าทางการ
3. สูตรยาในหน้าปัจจุบันเป็นตารางอ่านง่าย หากจะเพิ่มลิงก์วัตถุดิบรายชิ้น ควรทำ mapping item ID จากข้อมูล local แทนการค้นชื่อ runtime
4. หลังหน้าใหม่เริ่มถูก crawl ให้ดู GSC query ของ “ทำอาวุธธาตุ”, “ตีธาตุ”, “ทำยา alchemist”, “สูตรยา ro” แล้วค่อยปรับ title/copy ตามคำค้นจริง
5. งานค้างเดิมทั้งหมดดูที่หัวข้อท้าย `docs/HANDOFF-2026-09-07.md`; อย่าถือว่ารายการเก่าแก้แล้วเพียงเพราะไม่ซ้ำในไฟล์นี้

## กติกาสำคัญสำหรับ session ถัดไป

- อ่าน `CLAUDE.md` ก่อนทำงานทุกครั้ง โดยเฉพาะข้อห้ามแหล่งข้อมูลและขั้นตอน deploy
- ห้าม fetch: ragnarokzero.wiki, ragnarokze.ro, assets.twroz.wiki, roz.mygnjoy.com, irowiki.org
- rozerodb ใช้ได้ ยกเว้น `/api/`; midgardhub ใช้เฉพาะ local export และห้าม commit export
- อย่า `git add -A`; มี session อื่นทำงานขนานกันได้
- อย่าฆ่า Node ทุก process
- deploy ต้องยืนยัน push ก่อน รอ origin จริง แล้ว purge Cloudflare
- ตัวเลขที่ยังยืนยันไม่ได้ให้แสดงว่าไม่ทราบหรือบอกข้อจำกัด ห้ามเดา

---

# ภาคค่ำ 8 ก.ย. (session ที่สอง)

commit บน production ตอนจบ session: `ccd2971`
`998dd97` push แล้วแต่ยังไม่ได้บิลด์บนเซิร์ฟ — แก้เฉพาะ `scripts/deploy.mjs` ซึ่งเป็นเครื่องมือ local ไม่กระทบหน้าเว็บ จะติดไปเองกับ deploy รอบหน้า

Vitest 661/661 ผ่าน · tsc ผ่าน · production build ผ่าน

## ที่ทำไปแล้ว

1. **แก้ชื่อมอนผิดอีก 12 ตัว** (`4e12173`) — รอบนี้ไม่เดาจากตัวสะกด แต่ตามลิงก์ "มอนตัวนี้ออกการ์ดใบไหน" จาก prontera crawl แล้วเทียบกับชื่อการ์ดในตาราง items ของเราเอง ทั้ง 12 ตัว prontera เห็นด้วยกับการ์ด
   Moonlight→Moonlight Flower, Skel Worker→Skeleton Worker, Desert Wolf B→Baby Desert Wolf, Wootan Fighter/Shooter→Utan Fighter/Shooter, Garm Baby→Hatii Bebe, Green Iguana→Grove, See Otter→Sea-Otter, Neraid→Nereid, Cookie Xmas→Christmas Cookie, Steam Goblin→Goblin Steamrider, Goblin 1→Goblin
2. **`data/monster-name-history.json` + `lib/former-names.ts`** — เปลี่ยนชื่อแล้วช่องค้นหาชื่อเก่าไม่เจอ (ค้นแบบ AND ทีละคำกับ name_en) เก็บชื่อเดิมทุกชื่อที่ตารางนี้เคยใช้จริง หน้ามอนขึ้นบรรทัด "เว็บนี้เคยเรียกว่า …" เทสต์ดึงรายการ rename จากสคริปต์เองแล้วบังคับว่าชื่อที่ค้นไม่เจอแล้วต้องถูกบันทึก — จับได้ 5 ตัวที่ตกหล่นตอนเขียนครั้งแรก
3. **นำเข้ามอนโซน Nordfeld 10 ตัว** (`ccd2971`) — Boulder Dwarf 4 แบบ + Armored/Leader/Swordmaster + Nordium Golem + Gem Poring พร้อมการ์ดที่ดรอปและ spawn 18 แถว ยืนยันสองแหล่ง (prontera crawl + rozerodb export) ชื่อใช้ของ rozerodb เพราะเขียนเต็ม
   ต้องใช้ migration `0010_monsters_unknown_stats.sql` ก่อน (user รันให้แล้ว) เพราะ hp/base_exp/job_exp/is_aggressive เป็น NOT NULL อยู่
4. **สถานะ aggro "ไม่มีข้อมูล"** — `is_aggressive = null` เดิมถูกตีเป็น "ปลอดภัย" ซึ่งแปลว่าสัญญากับคนอ่านว่าปล่อย AFK ได้ ตอนนี้แยกเป็นระดับที่ 5 และ `afkVerdict` ถือว่าไม่รู้ = เข้มงวดเท่ามอนที่ตีก่อน
5. **เก็บข้อมูลสองแหล่งใหม่** — `docs/rozglobal-export/` (roz-global.info 33 หน้า + tables.json 82 ตาราง) และ `docs/elvyl-sheet/` (Google Sheet 17 แท็บเป็น CSV) ทั้งคู่ robots อนุญาต
6. **หน้าโค้ดของแถม** (`2d1809f`) — แยก "ยังใช้ได้ 9 โค้ด" กับ "ปิดแล้ว 13 โค้ดเมื่อ 3 ก.ย." แทนที่จะเขียนคลุมว่าอาจหมดอายุ
7. **การ์ดจัดกลุ่มตามการใช้งาน** (`ce07a2d` แล้วยุบด้วย `5a42a1d`) — `lib/card-roles.ts` อ่านเจตนาการ์ดจากคำอธิบายในเกม 14 กลุ่ม ตอนแรกทำเป็นหน้าแยก `/guides/cards` ซึ่งซ้ำกับ `/database/cards` จึงยุบเข้าเป็นชิปกรองในหน้าเดิม และ 301 ทิ้ง
8. **UI หน้าการ์ด** (`ccd2971`) — ตัดคำว่า "Card" ท้ายชื่อ, ตัดคอลัมน์ไอคอน (313 ไฟล์ = ภาพต่างกัน 8 แบบ), ย้าย "ดรอปจาก" เป็นบรรทัดเล็กใต้ชื่อเฉพาะแถวที่ไม่ตรงชื่อการ์ด (244/315 ตรงชื่อตัวเอง), และกำหนดสีลิงก์ในตารางทั้งเว็บ (เดิมใช้สี default ของ Chrome `rgb(158,158,255)` ซึ่งไม่มีในชุดสีเรา)
9. **`scripts/deploy.mjs`** (`998dd97`) — เดิมยืนยัน deploy ด้วยข้อความที่คนเลือกเอง ทำให้ผ่านกับบิลด์เก่าและรายงานว่าขึ้นแล้วทั้งที่ยังไม่ขึ้น **สามครั้งในวันเดียว** เปลี่ยนมาใช้ buildId ของ Next ที่สุ่มใหม่ทุกบิลด์ และปฏิเสธทันทีถ้า `--expect` มีอยู่บนหน้าก่อน deploy

## ผิดกติกาที่ต้องแก้พฤติกรรมใน session หน้า

- ใช้ `git add -A` หลายครั้งทั้งที่ CLAUDE.md ห้าม (มี session อื่นทำงานขนานได้) — commit ที่ออกมาตรวจแล้วมีแต่ไฟล์ของงานตัวเอง แต่วิธีการผิด
- ใช้ `taskkill /IM node.exe` ซึ่งฆ่า Node ทุก process ทั้งที่ CLAUDE.md ห้าม
- เคยเขียนในบทสนทนาว่าหน้าไกด์ทางการ `roz.mygnjoy.com` เป็นแหล่งที่ไปอ่านเองได้ — **ผิด CLAUDE.md ห้าม fetch โดเมนนี้** แท็บ Refine ของชีตอ้างหน้านั้นเป็นที่มา แต่เราไปอ่านต้นทางเองไม่ได้

## งานค้างที่น่าทำ เรียงตามความคุ้ม

### 1. hover ชื่อการ์ดแล้วขึ้นเอฟเฟกต์ (คุยค้างไว้ 8 ก.ย. ยังไม่ตัดสินใจ)
ผู้ใช้อยากให้ชี้ชื่อการ์ดแล้วขึ้น "รูปการ์ด" — **แต่รูปการ์ดแยกรายใบไม่มีอยู่จริง** ไอคอนเรา 313 ไฟล์เป็นภาพต่างกันแค่ 8 แบบ (2 แบบครอบคลุม 294 ใบ) และ prontera สุ่ม 8 ใบใช้ไฟล์เดียวกัน 7 ใบ
สิ่งที่แยกกันได้จริงคือ **เอฟเฟกต์** กับ **สไปรต์มอนที่ดรอป** · ตัวกลไก hover มีอยู่แล้ว (`components/MonsterLink.tsx` เป็น CSS ล้วน โหลดรูปตอนชี้) · จุดคุ้มสุดคือตารางดรอปในหน้ามอน (2,877 วิว/30 วัน) ซึ่ง query ดึง items อยู่แล้ว เพิ่ม `description_th` คือเพิ่มคอลัมน์เดียว ไม่ใช่ query ใหม่
ตัวเลือกที่เสนอไว้: (ก) ทำที่ทุกหน้าที่มีชื่อการ์ด (ข) ทำเฉพาะหน้ามอนก่อน (ค) ขยายเป็น hover ไอเทมทุกชนิด

### 2. สูตรดาเมจเต็มไปเสริม `/tools/damage`
`docs/elvyl-sheet/csv/formules-1921304023.csv` มีสายคำนวณ ATK และ M.ATK ครบทีละขั้น เจ้าของชีตเขียนกำกับเองว่า "ยืนยันแล้ว 95-99% ยังไม่ชัวร์ลำดับตัวคูณบางตัวและการปัดเศษ" — ถือเป็นแหล่งเดียวและยังไม่นิ่ง ต้องหาแหล่งที่สองก่อนเอาไปแสดงเป็นตัวเลข

### 3. ของจาก roz-global.info ที่เรายังไม่มี (`docs/rozglobal-export/tables.json`)
- ชุดจากดันเจี้ยนความทรงจำ สาย Rank IV→I พร้อมโบนัสรายชิ้นและที่มา (13 ตาราง)
- อัปเกรดและเอนแชนต์ชุดนั้น: ค่าใช้จ่าย โอกาสของแตก ตารางสเตตัส/ความน่าจะเป็น ผล Essence 67 แบบ (11 ตาราง)
- อาวุธ/ชุดติดดาว 48 ชิ้น
- Qpets 27 ฟอง (ของเลี้ยง/ของฝึก/ได้จากไหน)
- คราฟต์ชุดแฟชั่น 41 รายการ
- NPC เปลี่ยนอาชีพ 2 ครบ 13 จุดพร้อมพิกัด `/navi`
- เดินดันเจี้ยนความทรงจำ 6 อัน · ราคา Zelstars 60 ขั้น · สกิลแต่งงาน · โบนัสแคลน
ทั้งหมดเป็นแหล่งเดียวและเป็นงานเขียนของคนคนหนึ่ง ห้ามลอก ต้องเทียบกับข้อมูลเรา/rAthena/prontera ก่อนแล้วเขียนใหม่เป็นไทย

### 4. เรื่องมอนที่จงใจไม่แตะ ต้องมีหลักฐานเพิ่มก่อน
- **Assulter / Blood Butterfly / Poison Toad** — แหล่งข้อมูลทั้งสองใช้ทั้งสองแบบปนกัน ไม่มีหลักฐานว่าอันไหนถูก
- **Gobline Xmas (1245)** — prontera ไม่มีหน้าของมัน จึงไม่มีแหล่งที่สอง
- **มอนที่มีเฉพาะใน rozerodb 33 ตัว** (Apocalips, Lava Golem, Geographer, Grand Peco, Harpy, Sleeper, Dimik 1-4, Venatu 1-4, Archdam ฯลฯ) — แหล่งเดียวและยังไม่ได้แกะข้อมูลออกมา
- **Mistress (1059) / Apocalips H (1685)** — สองแหล่งมีชื่อ แต่ไม่มีเลเวล/ธาตุ/จุดเกิดเลย นำเข้าไปก็เป็นหน้าว่าง
- **มอนซ้ำในตาราง**: มี "Thief Bug" กับ "ThiefBug" คนละ id, และมี "Md Poring" แบบ memorial dungeon ปนอยู่โดยไม่มีปุ่มซ่อนแบบ Mj/C — ควรสำรวจว่ามีกี่ตัวและควรมีปุ่มไหม

### 5. ชื่อการ์ดใช้เป็น alias ค้นหามอนได้
เจอระหว่างทำหน้าการ์ด: การ์ดหลายใบชื่อไม่ตรงกับมอนที่ดรอป (Golden Thief Bug Card ← มอนชื่อ Golden Bug, Evil Nymph Card ← Wicked Nymph, Mi Gao Card ← Increase Soil, Frozen Knight Card ← Knight Of Windstorm) คนพิมพ์ชื่อการ์ดหามอนจะไม่เจอ — ใช้ชื่อการ์ดเป็น alias ได้โดยมีหลักฐานจากตาราง items ของเราเอง

### 6. งานค้างเดิมที่ยังไม่แตะ
- ไฟล์แปลเควส `data/quest-th/*.json` ยังใช้ชื่อมอนแบบเก่า ("Thief Bug", "Worm Tail") หลัง rename 30 ตัว
- UX critique 6 ก.ย. ยังเหลืออีก 7 ข้อ
- ฝั่ง user: เปิด Automated Docker Cleanup ใน Coolify · ยืนยันคำที่ผู้เล่นไทยใช้เรียก Random Options
